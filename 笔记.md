# StarTail-Mall 项目结构分析

## 一、项目总体判断

当前仓库不是单一前端应用，而是一个包含两个 uni-app 子项目的商城项目：

- `StarTail-Mall-User`：星轨商城客户端，面向用户浏览商品、分类、搜索、购物车、我的页面。
- `StarTail-Mall-Admin`：星轨商城后台管理端，基于 DCloud `uni-admin` 模板，使用 uniCloud、uni-id、uni-stat 等能力。
- 根目录 `package.json`：只声明了 `prettier` 和 `@dcloudio/uni-ui`，没有运行脚本，不能直接在根目录执行 `npm run dev`。

项目当前更接近 HBuilderX 创建/导入的 uni-app 工程，而不是已经完整 CLI 化的 npm 工程。尤其是 `StarTail-Mall-User` 根目录没有前端 `package.json`，只能依赖 HBuilderX 内置的 uni-app 编译能力，或后续补齐 CLI 依赖后再用 npm 启动。

## 二、目录结构

```text
StarTail-Mall/
├─ package.json
├─ package-lock.json
├─ StarTail-Mall-User/
│  ├─ App.vue
│  ├─ main.js
│  ├─ manifest.json
│  ├─ pages.json
│  ├─ project.config.json
│  ├─ components/
│  ├─ pages/
│  ├─ static/
│  ├─ utils/
│  ├─ uni_modules/
│  └─ server/
└─ StarTail-Mall-Admin/
   ├─ App.vue
   ├─ main.js
   ├─ manifest.json
   ├─ pages.json
   ├─ package.json
   ├─ README.md
   ├─ admin.config.js
   ├─ vue.config.js
   ├─ postcss.config.js
   ├─ common/
   ├─ components/
   ├─ i18n/
   ├─ js_sdk/
   ├─ pages/
   ├─ store/
   ├─ static/
   ├─ uni_modules/
   ├─ uniCloud-aliyun/
   └─ windows/
```

## 三、客户端 StarTail-Mall-User

### 1. 应用入口

- `main.js`：兼容 Vue2/Vue3 条件编译，当前 `manifest.json` 中指定 `vueVersion` 为 `3`，实际应走 Vue3 的 `createSSRApp(App)`。
- `App.vue`：全局生命周期和字体样式。
- `manifest.json`：应用名为“星轨商城-客户端”，配置了 App、小程序平台，`mp-weixin.appid` 为空。
- `pages.json`：配置页面、tabBar 和 easycom。

### 2. 页面结构

主要页面：

- `pages/index/index.vue`：首页。
- `pages/shop/category.vue`：商城分类页。
- `pages/car/car.vue`：购物车。
- `pages/my/my.vue`：我的。
- `pages/Search/Search.vue`：搜索页。
- `pages/Search/Search-list.vue`：搜索结果页。

tabBar 共 4 个入口：首页、商城、购物车、我的。

### 3. 组件结构

- `components/index/`：首页相关组件，如轮播、推荐、热门、分类 tab 等。
- `components/common/`：商品卡片、商品列表、卡片容器。
- `components/mobile-nav-bar/`：移动端导航栏。

### 4. 数据请求与本地接口

客户端请求封装在：

```text
StarTail-Mall-User/utils/api/request.js
```

默认接口地址：

```js
baseUrl: "http://localhost:3001/api"
```

本地服务在：

```text
StarTail-Mall-User/server/
```

它是一个 Express 服务，默认端口 `3001`，脚本为：

```json
"start": "nodemon ./bin/www"
```

注意：服务端路由实际把商品 mock 接口写在根路由 `routes/index.js` 中，例如 `/api/index_list/recommend`、`/api/index_list/game` 等。这个服务主要提供首页分类商品 mock 数据，不是完整商城后端。

## 四、后台 StarTail-Mall-Admin

### 1. 应用性质

后台是 `uni-admin` 模板改造项目，README 仍是 uni-admin 模板说明。它依赖：

- uni-app
- uniCloud
- uni-id
- uni-stat
- Vuex
- vue-i18n
- uni_modules 插件生态

### 2. 入口与配置

- `main.js`：注册 Vuex store、uni-admin 插件、国际化。
- `App.vue`：初始化登录状态、主题、uniCloud 扩展存储上传逻辑。
- `admin.config.js`：后台登录页、首页、错误页、顶部栏和侧边栏配置。
- `pages.json`：后台页面、系统管理页面、统计页面、登录/注册页面、窗口布局。
- `manifest.json`：应用名为“星轨商城-后台管理”，H5 路由 base 是 `/admin/`，Vue 版本为 3。

### 3. 页面模块

主要模块：

- `pages/index/`：后台首页。
- `pages/system/`：菜单、权限、角色、用户、应用、标签、安全日志。
- `pages/uni-stat/`：访问统计、用户统计、设备统计、错误统计、支付订单统计。
- `pages/demo/`：图标、表格示例。
- `pages/error/`：404 页面。

### 4. 云开发资源

后台包含：

```text
StarTail-Mall-Admin/uniCloud-aliyun/
```

其中包括：

- `cloudfunctions/`：uni-id、短信、统计、升级中心、扩展存储等云函数/云对象。
- `database/`：大量 schema、index、init_data 文件，如 `uni-id-users`、`uni-id-roles`、`uni-pay-orders`、`uni-stat-*` 等。

这说明后台要完整运行，除了前端编译，还需要 HBuilderX 或 uniCloud 控制台关联云服务空间、上传云函数、初始化数据库。

## 五、当前如何运行

### 方案 A：用 HBuilderX 运行，最符合当前项目形态

1. 打开 HBuilderX。
2. 分别导入两个目录：
   - `StarTail-Mall-User`
   - `StarTail-Mall-Admin`
3. 运行客户端前，先启动本地 Express mock 服务：

```bash
cd StarTail-Mall-User/server
npm install
npm start
```

服务默认运行在：

```text
http://localhost:3001
```

4. 在 HBuilderX 中运行 `StarTail-Mall-User`：
   - 运行到浏览器：适合看 H5 页面。
   - 运行到微信开发者工具：需要配置 `manifest.json` 里的 `mp-weixin.appid`。

5. 在 HBuilderX 中运行 `StarTail-Mall-Admin`：
   - 运行到浏览器：适合后台 H5 管理端。
   - 若要使用登录、权限、统计等功能，需要关联 uniCloud 服务空间，并初始化 `uniCloud-aliyun/database`、上传相关云函数。

### 方案 B：当前仓库直接 npm 运行的情况

根目录没有 `dev` 脚本，不能运行：

```bash
npm run dev
```

`StarTail-Mall-Admin/package.json` 也只有：

```json
"test": "echo \"Error: no test specified\" && exit 1"
```

所以当前状态下，前端不能直接通过 npm 启动。唯一已经可 npm 启动的是客户端 mock 服务：

```bash
cd StarTail-Mall-User/server
npm install
npm start
```

## 六、如何重构成 npm run dev 可运行

目标是把两个 HBuilderX 风格 uni-app 项目补齐为 CLI/Vite 风格项目，让每个子项目都有自己的 `package.json`、`vite.config.js` 和运行脚本。

### 1. 推荐重构方向

建议保留现有目录结构，把两个子项目分别 CLI 化：

```text
StarTail-Mall-User/package.json
StarTail-Mall-User/vite.config.js
StarTail-Mall-Admin/package.json
StarTail-Mall-Admin/vite.config.js
```

根目录可以再做一个 workspace 或聚合脚本：

```json
{
  "scripts": {
    "dev:user": "npm --prefix StarTail-Mall-User run dev:h5",
    "dev:admin": "npm --prefix StarTail-Mall-Admin run dev:h5",
    "dev:server": "npm --prefix StarTail-Mall-User/server start"
  }
}
```

如果要一个命令同时跑前台和服务端，需要新增并发工具，例如 `concurrently`，这属于新增第三方依赖，实施前应确认。

### 2. 每个 uni-app 子项目需要补齐的核心依赖

Vue3 + uni-app CLI 通常需要：

```json
{
  "dependencies": {
    "@dcloudio/uni-app": "latest",
    "@dcloudio/uni-h5": "latest",
    "@dcloudio/uni-mp-weixin": "latest",
    "@dcloudio/uni-ui": "latest",
    "vue": "latest"
  },
  "devDependencies": {
    "@dcloudio/vite-plugin-uni": "latest",
    "vite": "latest"
  }
}
```

后台还需要根据现有代码补充：

```json
{
  "dependencies": {
    "vue-i18n": "latest",
    "vuex": "latest"
  }
}
```

更稳妥的做法不是全部用 `latest`，而是先用 HBuilderX 当前内置 uni-app 版本或官方 CLI 模板版本对齐，避免 uni_modules 与编译器版本不匹配。

### 3. 典型脚本

客户端可以设计为：

```json
{
  "scripts": {
    "dev:h5": "uni -p h5",
    "dev:mp-weixin": "uni -p mp-weixin",
    "build:h5": "uni build -p h5",
    "build:mp-weixin": "uni build -p mp-weixin"
  }
}
```

如果采用 Vite 官方入口，也可能是：

```json
{
  "scripts": {
    "dev:h5": "vite --host 0.0.0.0",
    "build:h5": "vite build"
  }
}
```

实际选择要以安装的 DCloud CLI 版本为准。

### 4. vite.config.js 示例方向

每个子项目需要：

```js
import { defineConfig } from 'vite'
import uni from '@dcloudio/vite-plugin-uni'

export default defineConfig({
  plugins: [uni()]
})
```

### 5. 重构注意点

- `StarTail-Mall-User` 缺前端 `package.json`，这是 CLI 化的第一步。
- `StarTail-Mall-Admin` 虽有 `package.json`，但它是插件市场模板信息，不是可运行 npm 脚本配置，需要改造成真实工程依赖配置。
- `uni_modules` 已经在项目中，CLI 编译时仍可识别，但版本要和编译器匹配。
- 后台的 uniCloud 功能不能只靠 `npm run dev` 完成，还需要云服务空间、数据库 schema 初始化、云函数上传。
- 用户端的 API 目前依赖 `StarTail-Mall-User/server`，前端启动前要先启动这个服务，否则首页分类数据请求会失败。
- 若要微信小程序运行，还需要配置 `manifest.json` 中的 `mp-weixin.appid`，并用微信开发者工具打开编译产物。

## 七、建议后续改造顺序

1. 先确认目标运行端：只跑 H5，还是同时跑微信小程序。
2. 先 CLI 化 `StarTail-Mall-User`，因为它依赖较少，风险低。
3. 给 `StarTail-Mall-User` 增加前端 `package.json`、`vite.config.js`、`dev:h5`、`dev:mp-weixin` 脚本。
4. 验证用户端 H5 能访问 `http://localhost:3001/api/index_list/recommend`。
5. 再 CLI 化 `StarTail-Mall-Admin`，并单独处理 uniCloud 关联、数据库初始化、云函数上传。
6. 最后再考虑根目录聚合脚本，例如 `npm run dev:user`、`npm run dev:admin`、`npm run dev:server`。

## 八、当前结论

当前项目可以运行，但推荐先用 HBuilderX。前台需要额外启动本地 Express mock 服务；后台需要 HBuilderX/uniCloud 生态支持。若要改造成 `npm run dev`，不是简单加一行脚本，而是要把两个 uni-app 子项目补齐为 CLI 工程，并补充 DCloud CLI/Vite 相关依赖。

## 九、改造进度（2026-08-24）

本节为当前实现状态，优先级高于前文的初始分析。前文关于“客户端没有 `package.json`”和“根目录不能执行 `npm run dev`”的描述已失效。

### 1. npm 运行入口已补齐

- 根目录 `package.json` 已提供后台、客户端 H5、微信小程序及本地 mock 服务的聚合脚本。
- `npm run dev`：启动 `StarTail-Mall-Admin` 的 Vue3 + Vite 后台，默认端口为 `5173`。
- `npm run dev:user:h5`：启动客户端 H5。
- `npm run dev:user:mp-weixin`：编译客户端微信小程序。
- `npm run dev:user-server`：启动 Express mock 服务。
- `StarTail-Mall-User/package.json` 和 `vite.config.js` 已存在，客户端可通过 uni-app CLI 编译。

首次运行需分别安装依赖：

```bash
npm --prefix StarTail-Mall-Admin install
npm --prefix StarTail-Mall-User install
npm --prefix StarTail-Mall-User/server install
```

### 2. 后台管理端改造

- 后台入口已改为独立 Vue3 + Vite 管理端，不再依赖 uni-admin 页面入口。
- 页面拆分为侧边导航、指标区、数据面板和详情抽屉等组件。
- `src/services/adminData.js` 成为后台 mock 数据读取入口，页面通过异步加载数据并覆盖加载、空数据和失败重试状态。
- 当前 `src/data/modules.js` 仍是 mock 数据。接入真实后端时应替换服务层，不要在页面组件内直接请求或拼接接口地址。

### 3. 客户端商城改造

- `pages/shop/category.vue` 已从空页面改为可用分类页，支持分类切换、商品浏览与加入购物车。
- 分类页 mock 商品数据位于 `pages/shop/category.data.js`，图片复用仓库已有静态资源。
- `pages/car/car.vue` 已实现本地购物车展示、商品勾选、全选、数量调整、删除和金额汇总。
- 购物车暂存于 uni-app 本地存储，键名为 `startail-cart`；结算按钮目前只展示待接入提示，尚未创建订单或调用支付。

### 4. 当前后端与 uniCloud 计划

- 当前可运行的业务后端是 `StarTail-Mall-User/server`，采用 Node.js + Express，职责是提供商品 mock 接口。
- `StarTail-Mall-Admin/uniCloud-aliyun` 保留 uniCloud、uni-id、统计和支付模板能力，但尚未和当前 Vue3 后台或客户端业务页面建立真实数据链路。
- uniCloud 可使用免费额度进行开发和测试；云函数、数据库、存储、流量、短信等超出免费额度后按量计费。微信支付还需要独立的微信商户平台能力与费率。
- 正式接入时，建议以 uniCloud 云函数作为商品、购物车、订单和管理员操作的可信数据边界，客户端不能直接信任或提交价格、库存、管理员角色和用户身份。

### 5. 已完成提交

- `9117ca2 feat: 拆分后台管理端组件结构`
- `2af7aab feat: 接入后台数据服务状态`
- `8c8f5e9 feat: 完成客户端商品分类页`
- `9094870 feat: 完成客户端购物车交互`

每个节点均已在本地提交。当前网络代理阻断 GitHub 连接，`git push origin sytuni` 返回 `CONNECT tunnel failed, response 404`，因此上述提交尚未推送到远端。

### 6. 验证与限制

- 已执行 JavaScript 语法检查与 `git diff --check`。
- 后台 `npm run build` 与客户端 `npm run build:mp-weixin` 暂未通过，直接原因是当前环境尚未安装子项目依赖，分别缺少 `vite` 与 `uni` 命令。
- 安装依赖后，应分别执行 `npm run build:admin`、`npm run build:user:mp-weixin`，并使用微信开发者工具打开小程序编译产物进行真机或模拟器验证。

### 7. 依赖安装验证（2026-08-24）

- 三个子项目当前均没有安装 `node_modules`。
- 当前 npm 默认 registry 为内网地址 `http://172.30.234.104:8081/repository/npm-group/`，安装时无法获取 `@dcloudio/uni-app`，返回 `E404`。
- 已使用命令级 `--registry=https://registry.npmmirror.com` 和 `--registry=https://registry.npmjs.org` 重试，均未修改全局或项目 npm 配置；两个公开 registry 同样返回 `E404`。
- 对官方 registry 执行 `npm ping` 时，`/-/ping` 也返回 `E404`；甚至查询 `vue`、`vite` 元数据也失败。结论是当前运行环境的 npm 网络请求被代理层拦截或改写，不能据此判断依赖版本是否有效。
- 因依赖无法下载，后台 Vite 构建、客户端 uni-app 小程序构建和浏览器验证均待可访问 npm registry 的网络环境恢复后执行。
